# This directory is used to store data related to each server. Each server will have its own folder named after its ID, containing files that store tournament configurations, player registrations, and other relevant data.